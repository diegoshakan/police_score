<div class="mt-5">
  <h3> Mês: <%= (Date.current - 1.month).strftime("%B") %> </h3>
</div>

<table class="table table-striped mt-5">
  <thead>
  <tr class="text-center">
    <th> Posto/Graduação</th>
    <th> Nome</th>
    <th> Pontuação </th>
    <th> Ação </th>
  </tr>
  </thead>
  <tbody>
  <% @policemen.sort_by(&:score_last_month).reverse.each do |policeman| %>
    <div id="<%= dom_id policeman %>">
      <tr class="text-center">
        <td> <%= policeman.position %> </td>
        <td> <%= policeman.name %> </td>
        <td> <%= policeman.score_last_month %> </td>
        <td> <%= link_to "Show this policeman", "../home/#{policeman.id}", data: {turbo: false}, class: "btn btn-info btn-sm" %> </td>
      </tr>
    </div>
  <% end %>
  </tbody>
</table>

<%= link_to "Voltar", home_path %>