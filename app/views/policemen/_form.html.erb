<%= form_with(model: policeman) do |form| %>
  <% if policeman.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(policeman.errors.count, "error") %> prohibited this policeman from being saved:</h2>

      <ul>
        <% policeman.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :position, style: "display: block" %>
    <%= form.text_field :position %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
